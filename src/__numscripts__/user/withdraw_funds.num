vars {
  account $user
  monetary $withdraw_amount
}

send $withdraw_amount (
  source = $user
  destination = @world
)

set_tx_meta("action", "withdraw")
set_tx_meta("user", $user)
set_tx_meta("amount", $withdraw_amount)